import { PricerangePipe } from './pricerange.pipe';

describe('PricerangePipe', () => {
  it('create an instance', () => {
    const pipe = new PricerangePipe();
    expect(pipe).toBeTruthy();
  });
});
